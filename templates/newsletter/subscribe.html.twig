{#{% extends 'base.html.twig' %}

{% block title %}Formulaire newsletter{% endblock %}

{% block body %}
	<div class="container">
		<div class="card w-50 mt-3 m-auto">
			<div class="card-header p-3">
				<h1 class="m-0">Inscription Newsletter</h1>
			</div>
			<div class="card-body">#}
<div class="newsletter-form">
	{{ form_start(form, {attr: {class: "gap-3"}}) }}

{#	<div class="d-flex flex-column gap-3">#}
		<div class="d-flex flex-row gap-3">
			<div class="form-floating w-75">
				{{ form_widget(form.email) }}
				{{ form_label(form.email) }}
			</div>
			{{ form_widget(form.submit, {attr: {class: "btn-newsletter w-25"}}) }}
		</div>
		{{ form_widget(form.isRgpd) }}
{#	</div>#}
	{{ form_end(form) }}
</div>

			{#</div>
		</div>
	</div>
{% endblock %}
#}