{{ form_start(form, {attr: {class: "filter"}}) }}
	{{ form_row(form.q) }}
	<h4>Genres</h4>
	{{ form_row(form.genres) }}
	<h4>Prix</h4>
	<div class="row">
		<div class="col-md-6">
			{{ form_row(form.min) }}
		</div>
		<div class="col-md-6">
			{{ form_row(form.max) }}
		</div>
	</div>
	<div id="price-slider" data-min="{{ min }}" data-max="{{ max }}" class="mb-3"></div>
	<h4>Précommandes</h4>
	{{ form_row(form.preorder) }}

	<button type="submit" class="btn btn-primary w-100">Filtrer</button>
{{ form_end(form) }}